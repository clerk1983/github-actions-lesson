version: "1.1"
name: sum
env:
  baseUrl: http://localhost:8080
tests:
  example:
    steps:
      - name:
          url: ${{env.baseUrl}}/api/sum
          method: GET
          params:
            input1: 1
            input2: 2
          check:
            status: 200
            jsonpath:
              $.sum: 3
